def is_prime(number):
	if number < 2:
		return False
	elif number == 2 or number == 3:
		return True
	else:
		if number % 2 == 0: #removing the even numbers
			return False
def prime_num(primes):
	primes = list()
	for i in range (3, number):
		return primes
print (prime_num(30))
